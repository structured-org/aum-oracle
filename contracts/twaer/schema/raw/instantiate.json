{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InstantiateMsg",
  "type": "object",
  "required": [
    "aum_oracles",
    "maxbtc_core_contract",
    "mocked_maxbtc_supply",
    "owner",
    "publisher",
    "twa_window_seconds",
    "twaer_immutability_seconds"
  ],
  "properties": {
    "aum_oracles": {
      "description": "A list of AUM oracle instances from where the contract gets individual AUMs.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "maxbtc_core_contract": {
      "description": "The address of the maxBTC core contract.",
      "type": "string"
    },
    "mocked_maxbtc_supply": {
      "description": "The mocked maxBTC supply used instead of the real supply before the token is minted. To turn the real token supply usage on, the owner must set the maxbtc_denom in the config. If the supply is zero, instant exchange rate calculations return Decimal::one().",
      "allOf": [
        {
          "$ref": "#/definitions/Uint128"
        }
      ]
    },
    "owner": {
      "description": "Owner of the contract authorized to perform privileged operations.",
      "type": "string"
    },
    "publisher": {
      "description": "The address allowed to publish the TWAER.",
      "type": "string"
    },
    "twa_window_seconds": {
      "description": "Time window in seconds for TWA calculation (e.g., 86400 for 24 hours).",
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "twaer_immutability_seconds": {
      "description": "The minimal number of seconds required to pass between sequential TWAER publications.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    }
  },
  "additionalProperties": false,
  "definitions": {
    "Uint128": {
      "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
      "type": "string"
    }
  }
}
